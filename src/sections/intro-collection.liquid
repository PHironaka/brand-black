{%- assign collection = collections[section.settings.collection] -%}

<div class="featured-collection--intro-copy">
<p>{{ section.settings.related_title }} </p>
<a href="/collections/{{ section.settings.collection }}">Shop Now <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="feather feather-arrow-up-right"><line x1="7" y1="17" x2="17" y2="7"></line><polyline points="7 7 17 7 17 17"></polyline></svg></a>
</div>
 


{% schema %}
{
    "name": "Intro collection",
    "class": "intro-copy",

    "settings": [
      {
      "id": "related_title",
      "type": "text",
      "label": "Section title",
      "default": "Other fine products"
    },


      {
        "id": "collection",
        "type": "collection",
        "label": "Collection"
      }
    ],
    "presets": [
      {
        "name": "Intro collection",
        "category": "Collection"
      }
    ]
  }
{% endschema %}